# Medical-Chatbot-with-LLMs-LangChain-Pinecone-Flask-AWS
A medical chatbot built using LLMs + LangChain + Pinecone + Flask, using RAG to retrieve medical knowledge and generate accurate answers. The app is containerized with Docker and deployable on AWS for scalable, real-world use.
